#define _CRT_SECURE_NO_WARNINGS 
#include<stdio.h>
#include<stdlib.h>

int main10() {

	//野指针就是没有初始化的指针,指针的指向是随机的,不可以 操作野指针
	//int* p;//野指针，指向不明确
	//*p = 200;
	//printf("%d\n", *p);
	//system("pause");
	//return 0;
}